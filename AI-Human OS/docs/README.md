# Docs

This directory is split into two parts:

- [`canonical/`](canonical/) for current source-of-truth docs
- [`archive/`](archive/) for historical or superseded docs and diagrams

Use `canonical/` first.
