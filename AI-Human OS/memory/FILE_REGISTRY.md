# File Registry

## Purpose

Defines evolving interface contracts for files generated by the system.

This registry is:

* bootstrapped automatically by the system
* refined over time through validation
* used to enforce consistency across files

---

## System Behavior

* If a file has no entry:
  -> system creates one (bootstrap)

* If a file has an entry:
  -> system validates against it (strict mode)

* If mismatch occurs:
  -> pipeline fails

---

## Entry Structure

### file: <relative_path>

* responsibility:
* imports:
* exports:
* reads:
* writes:
* key_symbols:
* dependencies:
* surface_family:
* style_owner:
* class_hooks:
* prop_inputs:
* workflow_mode:
* action_owner:
* request_boundary:
* response_boundary:
* state_owner:
* server_authority_boundary:
* allowed_callers:

---

## Interface Contract

* inputs:

  * <name> (required|optional)

* outputs:

  * <event or effect>

---

## Symbol Rules

* allowed_symbols:

  * <symbol>

* forbidden_symbols:

  * <symbol>

---

---

## Notes

* This file is append-only (by system)
* Entries are created automatically
* You should NOT manually edit entries during normal operation
* Over time, this becomes the system's type memory

---

### file: shared/multiplayer/CreateRoomCommand.js

* responsibility: define the shared request contract for room creation so client and server agree on the create-room input shape without duplicating assumptions
* imports: none
* exports: default createCreateRoomCommand, createCreateRoomCommand, isCreateRoomCommand
* reads: none
* writes: none
* key_symbols: none
* dependencies: none
* surface_family: lobby-layout
* style_owner: client/ui/layout/LobbyLayout.css
* class_hooks: none
* prop_inputs: none
* workflow_mode: server_authoritative
* action_owner: none
* request_boundary: not_required
* response_boundary: not_required
* state_owner: none
* server_authority_boundary: only server/core/update/createRoomSession.js may create and register a joinable room
* allowed_callers: none

---

## Interface Contract

* inputs:

  * none

* outputs:

  * none

---

## Symbol Rules

* allowed_symbols:

  * none

* forbidden_symbols:

  * none

---

### file: shared/multiplayer/CreateRoomResult.js

* responsibility: define the shared response contract that exposes only the room creation result needed by the client, including joinable-room confirmation data
* imports: none
* exports: default createCreateRoomResult, createCreateRoomResult, isCreateRoomResult
* reads: roomCode, isJoinable
* writes: none
* key_symbols: roomCode, isJoinable
* dependencies: none
* surface_family: lobby-layout
* style_owner: client/ui/layout/LobbyLayout.css
* class_hooks: none
* prop_inputs: roomCode, isJoinable
* workflow_mode: server_authoritative
* action_owner: client/ui/layout/LobbyLayout.js
* request_boundary: client/ui/createRoomClient.js
* response_boundary: shared/multiplayer/CreateRoomResult.js
* state_owner: server/core/state/roomRegistry.js
* server_authority_boundary: only server/core/update/createRoomSession.js may create and register a joinable room
* allowed_callers: none

---

## Interface Contract

* inputs:

  * roomCode (optional)
  * isJoinable (optional)

* outputs:

  * none

---

## Symbol Rules

* allowed_symbols:

  * roomCode
  * isJoinable

* forbidden_symbols:

  * none

---

### file: server/core/domain/createRoomRecord.js

* responsibility: introduce the pure domain factory that derives a new room record and its initial joinable state from validated create-room input
* imports: none
* exports: default createCreateRoomRecord, createCreateRoomRecord, isCreateRoomRecord
* reads: none
* writes: none
* key_symbols: none
* dependencies: op_001
* surface_family: lobby-layout
* style_owner: client/ui/layout/LobbyLayout.css
* class_hooks: none
* prop_inputs: none
* workflow_mode: server_authoritative
* action_owner: none
* request_boundary: not_required
* response_boundary: not_required
* state_owner: none
* server_authority_boundary: only server/core/update/createRoomSession.js may create and register a joinable room
* allowed_callers: op_001

---

## Interface Contract

* inputs:

  * none

* outputs:

  * none

---

## Symbol Rules

* allowed_symbols:

  * none

* forbidden_symbols:

  * none

---

### file: server/core/state/roomRegistry.js

* responsibility: create the authoritative server-owned room registry that stores created rooms and their joinable availability
* imports: ../domain/createRoomRecord.js
* exports: default createRoomRegistry, createRoomRegistry, isRoomRegistry
* reads: none
* writes: none
* key_symbols: none
* dependencies: op_003
* surface_family: lobby-layout
* style_owner: client/ui/layout/LobbyLayout.css
* class_hooks: none
* prop_inputs: none
* workflow_mode: server_authoritative
* action_owner: client/ui/layout/LobbyLayout.js
* request_boundary: client/ui/createRoomClient.js
* response_boundary: shared/multiplayer/CreateRoomResult.js
* state_owner: server/core/state/roomRegistry.js
* server_authority_boundary: only server/core/update/createRoomSession.js may create and register a joinable room
* allowed_callers: op_003

---

## Interface Contract

* inputs:

  * none

* outputs:

  * none

---

## Symbol Rules

* allowed_symbols:

  * none

* forbidden_symbols:

  * none

---

### file: server/core/update/createRoomSession.js

* responsibility: add the update-layer operation that validates a create-room command, creates a room through the domain layer, and writes it into authoritative server state
* imports: ../../../shared/multiplayer/CreateRoomCommand.js, ../domain/createRoomRecord.js, ../state/roomRegistry.js
* exports: default createRoomSession, createRoomSession
* reads: { command, roomRegistry
* writes: none
* key_symbols: { command, roomRegistry
* dependencies: op_001, op_003, op_004
* surface_family: lobby-layout
* style_owner: client/ui/layout/LobbyLayout.css
* class_hooks: none
* prop_inputs: { command, roomRegistry
* workflow_mode: server_authoritative
* action_owner: none
* request_boundary: not_required
* response_boundary: not_required
* state_owner: none
* server_authority_boundary: only server/core/update/createRoomSession.js may create and register a joinable room
* allowed_callers: op_001, op_003, op_004

---

## Interface Contract

* inputs:

  * { command, roomRegistry (optional)

* outputs:

  * none

---

## Symbol Rules

* allowed_symbols:

  * { command, roomRegistry

* forbidden_symbols:

  * none

---

### file: server/core/render/projectCreateRoomResult.js

* responsibility: add the render projection that converts authoritative room creation state into the shared client-safe result payload
* imports: ../../../shared/multiplayer/CreateRoomResult.js, ../state/roomRegistry.js
* exports: default projectCreateRoomResult, projectCreateRoomResult
* reads: { roomCode
* writes: none
* key_symbols: { roomCode
* dependencies: op_002, op_005
* surface_family: lobby-layout
* style_owner: client/ui/layout/LobbyLayout.css
* class_hooks: none
* prop_inputs: { roomCode
* workflow_mode: server_authoritative
* action_owner: none
* request_boundary: not_required
* response_boundary: not_required
* state_owner: none
* server_authority_boundary: only server/core/update/createRoomSession.js may create and register a joinable room
* allowed_callers: op_002, op_005

---

## Interface Contract

* inputs:

  * { roomCode (optional)

* outputs:

  * none

---

## Symbol Rules

* allowed_symbols:

  * { roomCode

* forbidden_symbols:

  * none

---

### file: server/core/input/createRoomRequest.js

* responsibility: introduce the input boundary that receives the room creation request, parses it into the shared command shape, invokes the update layer, and returns the projected result
* imports: ../../../shared/multiplayer/CreateRoomCommand.js, ../render/projectCreateRoomResult.js, ../state/roomRegistry.js, ../update/createRoomSession.js
* exports: default createRoomRequest, createRoomRequest
* reads: { request, roomRegistry
* writes: none
* key_symbols: { request, roomRegistry
* dependencies: op_001, op_005, op_006
* surface_family: lobby-layout
* style_owner: client/ui/layout/LobbyLayout.css
* class_hooks: none
* prop_inputs: { request, roomRegistry
* workflow_mode: server_authoritative
* action_owner: none
* request_boundary: not_required
* response_boundary: not_required
* state_owner: none
* server_authority_boundary: only server/core/update/createRoomSession.js may create and register a joinable room
* allowed_callers: op_001, op_005, op_006

---

## Interface Contract

* inputs:

  * { request, roomRegistry (optional)

* outputs:

  * none

---

## Symbol Rules

* allowed_symbols:

  * { request, roomRegistry

* forbidden_symbols:

  * none

---

### file: server/main.js

* responsibility: expose the minimal server runtime entry that makes the room creation request boundary reachable from the browser-facing client
* imports: ./core/input/createRoomRequest.js, ./core/state/roomRegistry.js
* exports: default createServerRuntime, createServerRuntime
* reads: { roomRegistry
* writes: none
* key_symbols: { roomRegistry
* dependencies: op_007
* surface_family: lobby-layout
* style_owner: client/ui/layout/LobbyLayout.css
* class_hooks: none
* prop_inputs: { roomRegistry
* workflow_mode: server_authoritative
* action_owner: none
* request_boundary: not_required
* response_boundary: not_required
* state_owner: none
* server_authority_boundary: only server/core/update/createRoomSession.js may create and register a joinable room
* allowed_callers: op_007

---

## Interface Contract

* inputs:

  * { roomRegistry (optional)

* outputs:

  * none

---

## Symbol Rules

* allowed_symbols:

  * { roomRegistry

* forbidden_symbols:

  * none

---

### file: vite.config.js

* responsibility: wire the existing browser runtime to the server room-creation boundary so the lobby UI can reach the authoritative create-room flow during development
* imports: vite, @vitejs/plugin-react
* exports: default defineConfig
* reads: none
* writes: none
* key_symbols: none
* dependencies: op_008
* surface_family: lobby-layout
* style_owner: client/ui/layout/LobbyLayout.css
* class_hooks: none
* prop_inputs: none
* workflow_mode: server_authoritative
* action_owner: none
* request_boundary: not_required
* response_boundary: not_required
* state_owner: none
* server_authority_boundary: only server/core/update/createRoomSession.js may create and register a joinable room
* allowed_callers: op_008

---

## Interface Contract

* inputs:

  * none

* outputs:

  * none

---

## Symbol Rules

* allowed_symbols:

  * none

* forbidden_symbols:

  * none

---
